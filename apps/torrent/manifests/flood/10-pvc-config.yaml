apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flood-config
  namespace: torrent
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-homelab
  resources:
    requests:
      storage: 1Gi
