apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik
  namespace: kube-system
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`traefik.minh.pw`)
      kind: Rule
      services:
        - name: api@internal
          kind: TraefikService
