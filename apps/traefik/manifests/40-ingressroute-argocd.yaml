apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`argo.minh.pw`)
      kind: Rule
      services:
        - name: argocd-server
          port: 80
          passHostHeader: true
