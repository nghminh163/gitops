apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vaultwarden
  namespace: vaultwarden
spec:
  entryPoints:
    - websecure
  routes:
    # Web UI / API
    - match: Host(`vault.minh.pw`)
      kind: Rule
      services:
        - name: vaultwarden
          port: 80
  tls: {}
